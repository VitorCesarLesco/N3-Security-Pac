# Configuração para desenvolvimento - N3 Segurança
# Configuração simplificada para desenvolvimento local

# Database - usando H2 em memória para desenvolvimento
# Removido db=h2-mem para usar padrão

# HTTP - configuração para desenvolvimento
http-enabled=true
http-port=8080
hostname-strict=false
hostname-strict-https=false

# Desenvolvimento - removido start-dev pois conflita
# start-dev=true

# Observability
health-enabled=true
metrics-enabled=true

# Admin user para desenvolvimento
# KC_BOOTSTRAP_ADMIN_USERNAME=admin
# KC_BOOTSTRAP_ADMIN_PASSWORD=admin

# Configurações comentadas para produção
# The file path to a server certificate or certificate chain in PEM format.
#https-certificate-file=${kc.home.dir}conf/server.crt.pem

# The file path to a private key in PEM format.
#https-certificate-key-file=${kc.home.dir}conf/server.key.pem

# The proxy address forwarding mode if the server is behind a reverse proxy.
#proxy=reencrypt

# Do not attach route to cookies and rely on the session affinity capabilities from reverse proxy
#spi-sticky-session-encoder-infinispan-should-attach-route=false

# Hostname for the Keycloak server.
#hostname=myhostname
